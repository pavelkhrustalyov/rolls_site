const rollsContainer = document.getElementById('rolls_container');
const fullCartContainer = document.getElementById('full_cart');
const emptyCart = document.querySelector('#empty_cart');
const totalPrice = document.querySelector('.total-price');
const checkout = document.querySelector('#checkout');
const numberInput = document.querySelector('#number_input');
const form = document.forms[0];

export default {
    rollsContainer,
    fullCartContainer,
    emptyCart,
    totalPrice,
    checkout,
    numberInput,
    form
};